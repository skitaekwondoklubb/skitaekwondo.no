export enum Instructor {
    NotInstructor,          // Not instructor
    SkiFullTimeInstructor,  // Only Ski Taekwondo Klubb
    SkiHelperInstructor,    // Only Ski Taekwondo Klubb
    InstructorAtVinterleir  // Only Master
}